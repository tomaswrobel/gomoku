# These match what the Vercel Supabase integration provisions automatically.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Server-only: never expose to the client, never import outside src/lib/server/.
SUPABASE_SECRET_KEY=
